<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estudoteca</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav id="navbar">
        <ul>
            <li><a href="/">Books</a></li>
            {{#if session.userid}}
                {{#if session.admin}}
                    <li><a href="/books/userbooks">Add Livros</a></li>
                {{else}}
                    <li><a href="/books/cart">Carrinho</a></li>    
                {{/if}}
                <li><a href="/logout">Sair</a></li>
            {{else}}
                <li><a href="/login">Login</a></li>
                <li><a href="/register">Register</a></li>
            {{/if}}
        </ul>
    </nav>
    <div class="conteiner">
        {{#if messages.message}}
            <div class="message-conteiner">
                <p>{{messages.message}}</p>
            </div>
        {{/if}}
        {{{body}}}
    </div>
</body>
</html>
